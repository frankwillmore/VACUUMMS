# Running VACUUMMS in a jupyter notebook

Make sure that you have created a kernel based on a python environment, and that it includes matplotlib for inline plots. Plots can then be enabled by adding the following to a cell in Jupyter:

    %matplotlib inline
    import matplotlib as plt


